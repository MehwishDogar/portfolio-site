import React from 'react';
import StyledProgressBar from './StyledProgressBar';

const ProgressBar = () => (
  <StyledProgressBar>
    <div>HTML 5</div>
  </StyledProgressBar>
);

export default ProgressBar;
