import { css } from 'styled-components';

const centerParagraphStyles = css`
  text-align: center;
  font: inherit;
  font-size: 18px;
  display: block;
  margin-bottom: 10px;
`;

export default centerParagraphStyles;
